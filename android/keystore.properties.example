storeFile=../keystore/release.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=sshku
keyPassword=YOUR_KEY_PASSWORD
# Generate with: keytool -genkey -v -keystore keystore/release.jks -keyalg RSA -keysize 2048 -validity 10000 -alias sshku
